# Quality Metrics Platform

The Quality Metrics Platform is a lightweight, extensible system for collecting, storing, and visualizing software quality metrics. It is built with a Python stack, leveraging FastAPI for the API layer, PostgreSQL for durable storage, and Grafana for dashboards. Docker is used for containerization and easy deployment.

The platform provides a simple health check endpoint and a foundation for adding metric ingestion, aggregation, and reporting services.
